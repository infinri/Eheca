symfonycasts_reset_password:
    request_password_repository: App\Repository\ResetPasswordRequestRepository
    # How long in seconds the reset token is valid (1 hour)
    lifetime: 3600
    # Enable garbage collection to clean up expired tokens
    enable_garbage_collection: true
    # Throttle limit for reset requests (per IP per hour)
    throttle_limit: 5
