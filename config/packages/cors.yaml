nelmio_cors:
    defaults:
        allow_origin: ['http://localhost:5173'] # Vite/React default
        allow_credentials: true
        allow_headers: ['Content-Type', 'Authorization']
        expose_headers: ['Link']
        max_age: 3600
    paths:
        '^/': ~
